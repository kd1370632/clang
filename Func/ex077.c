#include<stdio.h>
void kazu(int x, int y, int z, int* ra, int* sm, );

main()
{
	int x, y, z, ra, sm;
	printf("®”‚ğ‚R‚Â“ü—Í");
	scanf("%d%d", &x, &y);
	kazu(x, y, z, &ra, &sm);
	printf("Å‘å’l=%d  Å¬’l=%d\n", ra, sm);

	void kazu(int x, int y, int z, int* ra, int* sm, )
	{

	}